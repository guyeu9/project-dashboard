## 详细修复方案

### 1. 恢复缺失的项目卡片

**文件**：`src/pages/Dashboard/index.tsx`
**修改**：
- 添加回"并行项目"卡片（显示所有项目数量）
- 添加回"未开始项目"卡片（显示进度为0的正常项目）
- 保持现有的延期项目和风险项目卡片
- 总共4个卡片，使用2x2网格布局

### 2. 修复控制台警告

#### Breadcrumb警告修复
**文件**：`src/components/Layout/index.tsx`
**修改**：
- 将旧的`Breadcrumb.Item`语法改为新的`items`属性语法
- 使用Ant Design 5.x推荐的breadcrumb配置方式

#### Dropdown警告修复  
**文件**：`src/components/NotificationDropdown/index.tsx`
**修改**：
- 将`overlay`属性改为`menu`属性
- 将`dropdownRender`改为`popupRender`
- 保持原有功能和样式不变

#### Form警告修复
**文件**：相关表单组件
**修改**：
- 检查`useForm`钩子的使用，确保正确连接到Form元素
- 添加缺失的`form`属性到Form组件

### 3. 修复全部项目卡片点击功能

**文件**：`src/pages/Dashboard/index.tsx`
**修改**：
- 为"全部项目"卡片添加点击事件
- 点击时设置`selectedFilter`为'all'，显示所有项目
- 添加选中状态的视觉反馈（蓝色边框）

### 4. 优化筛选逻辑

**文件**：`src/pages/Dashboard/index.tsx`
**修改**：
- 完善`filteredProjects`计算逻辑
- 支持多种筛选状态的组合
- 确保点击卡片能正确切换筛选状态

### 5. 界面布局优化

**文件**：`src/pages/Dashboard/index.tsx`
**修改**：
- 使用响应式网格布局（大屏幕4列，中等屏幕2列，小屏幕1列）
- 优化卡片样式和交互效果
- 保持统一的视觉风格